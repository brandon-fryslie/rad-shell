#!/usr/bin/env ruby

require 'optparse'
require 'methadone'
require 'radsh.rb'

# Rad shell executable

# functions
# profile
# - view profile
# - switch profile
# - list profile

# what is a profile?
# a profile is a collection tracked dotfiles
# - how do i keep track of these?
#   - each is in a repo in the .rad-shell directory
# - a profile also needs to keep track of the shell that is used
# - profile should have of way of only tracking files for specific programs
# -
# require 'optparse'
# require 'methadone'

class App
  include Methadone::Main
  include Methadone::CLILogging

  main do # Add args you want: |like,so|
    # your program code here
    # You can access CLI options via
    # the options Hash
  end

  description "rad-shell: make your shell rad"
  #
  # Accept flags via:
  # on("--flag VAL","Some flag")
  # options[flag] will contain VAL
  #
  # Specify switches via:
  # on("--[no-]switch","Some switch")
  #
  # Or, just call OptionParser methods on opts
  #
  # Require an argument
  # arg :some_arg
  #
  # # Make an argument optional
  # arg :optional_arg, :optional

  version Radsh::VERSION

  use_log_level_option :toggle_debug_on_signal => 'USR1'

  go!
end
